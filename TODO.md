# 未完成 / 已知边界

- 2026-08-10:定位并修掉 24 个根因(见 git log)
- 2026-08-11:联网人工审计(结论逐条核对为真),又修掉 3 项 + 1 个审计自己引入的误杀
- 2026-08-11(第二轮):4 个随机用例(電動歯ブラシ / ワイヤレスイヤホン / ゲーム機 / 服飾)
  发现并修掉 8 个问题,详见 git log 与文末"第二轮审计"

---

## 待办

### B. Google 对照基线还没跑通(已**默认关闭**)

`--google-benchmark` 默认 `False`,需要显式加 flag 才跑。代码与 39 个离线测试都保留。
关掉的理由不只是 API 没通,还因为**核心比较语义仍在打磨中** ——
不能让一个未验证的记分板产生误导性的战绩(它第一次就把单只右耳当我方最低价报了 win)。

真实 API 一直没通过,错误在三种状态间来回:

1. `Custom Search API has not been used in project ... or it is disabled`
2. 启用后变成 `Requests to this API customsearch method ... are blocked`(= API key 有 API restrictions)
3. 又退回第 1 种 —— 启用状态似乎没稳定传播

**要做的**(在你侧,代码无需改动):GCP Console → APIs & Services → 确认 Custom Search API 已启用;
→ Credentials → 该 API key → **API restrictions** 里加上 Custom Search API(或选 Don't restrict key)。

失败隔离已验证:取不到数就记 `error` + `verdict: n/a`,不影响推荐输出。

**在此期间用人工审计代替** —— 让带联网能力的 Claude 跑一遍同样的查询、核对结论。
实测这比 CSE 有用得多(见文末),但**不可复现、攒不出趋势**,所以两者不互相替代:
审计当阶段性体检,记分板当持续指标。

---

## 已修(2026-08-11)

### ✅ A. `get_price_statistics` 曾被配件污染

原来 `get_price_statistics("ニコン D850")` 返回 median ¥5,000 / min ¥370 ——
一台十几万日元的相机,中位数五千,因为样本里全是镜头盖、保护膜、书籍。

现在返回**本体行情**(`basis: "body_only"`),三层过滤:配件/残缺词表 → 产品身份 token
→ 价格下限(新增可选 `price_min` 参数)。实测同一查询 median **¥5,000 → ¥195,015**。
一并保留 `unfiltered`(未过滤原始统计)与 `excluded`(剔了多少条、按什么规则、每类一条样本),
让"过滤是不是过头了"查得出来。

没给 `price_min` 时若样本呈双峰(最高价 ≥ 中位数的 10 倍),返回 `warnings` 直接要求补下限重查。
prompt R11 要求模型必须照办。实测模型第一次调用就自己带上了 `price_min`。

### ✅ C. `benchmarks/` 不进版本库

已加进 `.gitignore`。它会记下检索词与完整 Google 结果,而仓库 URL 是公开的。
想让记分板历史进 git 就删掉那一行。

### ✅ D. 「最安値」≠「实际支付额」

审计发现的一整类没建模的东西:

- **メーカーキャッシュバック**:Braun 有"本体+替刃 同時購入で ¥3,500"(占 ¥43,960 的 **8%**,
  是记分板 win 阈值 2% 的四倍 —— 一个 win 完全可能被一次キャッシュバック反转);
  D850 在 2025 春有过"统一 ¥308,000 + ¥50,000 キャッシュバック"(实质 ¥258,000)。
- **楽天ポイント / スーパーDEAL**(最高 50% 还元),kakaku 的最安値完全不计入点数。
- **kakaku 的店铺覆盖是分商品的**:实测 9657cc 的比价列表里有楽天、Joshin,
  但没有 Amazon / ヨドバシ / ビックカメラ。

**修法**:不建模キャンペーン(会过时、要长期维护),而是把前提写进数据契约 ——
`get_new_and_newer_models` 返回 `price_basis: "cash_lowest_excl_campaigns"` + `price_basis_note`,
prompt R10 要求每次给价格对比都必须声明这一点、且**不许凭记忆编造具体活动名称与金额**。
实测生效:回答末尾出现「上記 kakaku 価格は現金最安値です。メーカーキャッシュバックや
ポイント還元は含まれておらず…購入前に当期キャンペーンをご確認ください」。

### ✅ 审计自己引入的误杀(修 A 时发现)

加了配件过滤后,`Nikon D850 …本体 NPSストラップ` ¥125,000 被当配件剔掉了 ——
**而它正是 agent 之前推荐的那台**。另一条 `…純正ストラップ付` ¥139,800 同样中招。

根因:配件名会出现在"本体+配件搭售"的标题里。改为条件排除:命中配件名时,
若标题含 `本体/ボディ/body` 或该配件名后跟搭售标记(`付/込/同梱/おまけ/あり`)则**不**排除。
纯配件 `Nikon D850 純正カメラストラップ` ¥1,390 仍被正确剔除。

> 这条值得记住:**过度过滤比漏过一个配件危险得多** —— 它会静默丢掉真正的便宜货,
> 而且查不出来。所以每个排除项都必须留原因和样本。

---

## 已知边界

### E. 官方直营店比 kakaku 贵,不是盲点(已验证)

审计时怀疑"漏了官方店"是渠道盲点,实测**不是**:
Braun 官方店 9657cc **¥67,980**,kakaku 最安 **¥43,960** —— 官方店贵 ¥24,020。
拿 kakaku 当新品锚点是对的,不需要额外接官方店。

### G. kakaku 只解析搜索结果第 1 页

返回里的 `coverage` 会如实交代(`catalog_models_parsed` / `total_hits_reported` / note),
但排名靠后的机型不会纳入比较。实测 "ニコン D850" 命中 738 件,第 1 页只有 6 个目录机型。

翻页会成倍增加请求数,与"低频克制取数"的原则冲突,所以先不做。

### H. 「洗浄器**対応**」≠「洗浄器**付き**」没有硬过滤

`対応` 只表示兼容,机器**不含**洗浄器(实测 ¥22,600 的「9577cc 洗浄器対応モデル」)。
它只进了 `AMBIGUOUS_KEYWORDS`(标注、不排除)和 prompt R9。

**刻意不硬过滤** —— 见上面"过度过滤"那条教训。

### I. family 归类对"只差数字"的命名无效

Nikon 的 D850 / D810 / D780 各自成一族(`_family_of` 剥不掉纯数字型号里的世代信息),
所以 D850 的后继型号只能给出 `newer_lookup: unknown`。

**审计验证这个"不知道"其实是最优解**:D850 确实不存在 DSLR 后继(Nikon 已转 Z 卡口),
硬报 Z8 当后继会误导(不同卡口、镜头资产不通用)。Braun / Apple 这类有明确系列名
(`シリーズ9 Pro+` / `AirPods Pro 3`)的品牌不受影响。

### J. Mercari 标题不写型番时无法核实是否同款

大量挂牌只写「AirPods Pro (第2世代) 本体」这种,没有 `MTJV3J/A`。
记分板的 `comparability` 会标 `unknown`,prompt R7 要求告诉用户"需向卖家确认",
但数据层面无解 —— 只能提示,不能替用户断定。

### K. 卖家名取不到

Mercari 搜索 API 不返回。标题/价/成色/图/品牌/分类都有。

---

## 顺序上的教训(给未来的自己)

2026-08-10 在核心比较语义(行类型 / 配置轴 cc-s / 世代轴 Pro-Pro+)还错着的时候,
先加了 Google 记分板。结果它的第一个判定是拿「AirPods Pro 第2世代 **右耳のみ**」
(单只右耳 ¥15,477)当我方最低价,痛快地报了个 `win`。

**先把被衡量的东西弄对,再建衡量工具。** 一个不诚实的基线比没有基线更糟 ——
它会让你以为自己在进步。

---

## 第二轮审计(2026-08-11,4 个随机用例)

用 電動歯ブラシ / ワイヤレスイヤホン / ゲーム機 / 服飾 四个品类各跑一次,联网核对结论。
**工具报出来的数字没有一个是错的**,错的都是"工具没查到、模型用自有知识补"的部分。
发现并修掉 8 个问题:

| # | 问题 | 修法 |
|---|---|---|
| 1 | `matches_product` 标题未归一化 → 带连字符型番(WF-1000XM5)恒不匹配,`get_price_statistics` 静默返回 count=0 | 标题用与 token 相同的规则归一化 |
| 2 | 数字兜底过宽 → `ES-LV9V` 只靠一个 "9" 就匹配任意标题 | 兜底只给含日文的系列词 |
| 3 | kakaku 搜索页无目录行就放弃(实测 WF-1000XM5 页面 0 目录行) | 跟进 K 链接读目录页,拿到 ¥24,605/2023-09/23店 |
| 4 | 未発売商品无警告 → EW-DA19(2026-09 発売预定)被当可买项推荐 | `_warnings_for` 加未来日期分支 + prompt R13 |
| 5 | "Nintendo Switch 2" 选中一个**游戏** ¥6,666 当主机价 | 有实义 token 必须落在 series 上,否则报无数据 |
| 6 | 品牌别名表缺 patagonia / nintendo 等 | 补 15 个品牌,并改成一对多(nintendo → 任天堂/ニンテンドー) |
| 7 | `recommend_deals` 完全绕过过滤(top1 是 ¥3,000 童装) | 接同一套过滤 + `filtered` 字段交代 |
| 8 | 工具无数据时模型仍编价格 | prompt R12 + **无来源价格检查**(确定性,不靠规则) |

**最值得记的一条**:#8 加了 prompt R12 之后,下一轮 Switch 2 用例**又**违反了一次
(写出工具返回里不存在的 ¥49,980)。所以才加了确定性的 `ungrounded_prices` 检查 ——
**prompt 规则管不住模型,检查才管得住。**

---

## 审计结果(2026-08-11,联网核对)

CSE 没通期间,用带联网能力的 Claude 核对 kaidoki 的实跑结论。**核对的结论全部为真**:

| kaidoki 说 | 外部核对 | 判定 |
|---|---|---|
| シリーズ9 Pro+ 是现行最新(2025-11) | 官方称"10年ぶりの大革新",**不存在シリーズ10** | ✅ 对 |
| Pro+ 発売日 2025-11 | 官方 2025年11月上旬発売 | ✅ 对 |
| 9657cc kakaku 新品最安 ¥43,999 | 当前 ¥43,960(19 店铺);官方店 ¥67,980 | ✅ 对且是真地板 |
| D850 `newer_lookup: unknown`,后继需人工确认 | **确实不存在 DSLR 后继**,Nikon 已转 Z 卡口 | ✅ 比"有/没有"都更准 |
| D850 新品 ¥279,980 是"旧型号尾货" | 2025-03 曾统一 ¥308,000 + キャッシュバック清库存 | ✅ 对 |

**没有发现一个错误结论。** 数据层是可信的。
唯一的新发现是 D 项 —— 不是"算错了",而是"少算了一整类东西"(渠道条件),已修。

**人工审计 vs CSE 记分板的分工**(实测得出):
- 审计能发现"这个价没算キャッシュバック""不存在シリーズ10"这类**语义**问题;
  CSE 只能正则抽价格,永远发现不了这些。
- 但审计**不可复现、不能累积趋势**,而且每次要花一轮对话。
- 所以:审计当**阶段性体检**,记分板当**持续指标**。两者不互相替代。
